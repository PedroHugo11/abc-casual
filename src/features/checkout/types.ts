export type Address = {
  id: string
  street: string
  number: string
  city: string
  state: string
  zip: string
  phone: string
}

export type Shipping = {
  id: string
  name: string
  price: number
  days: number
}